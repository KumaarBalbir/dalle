/* The code is exporting three components - `Card`, `Loader`, and `FormField` - from their respective
files. This allows other files to import and use these components in their own code. */
import Card from "./Card";
import Loader from "./Loader";
import FormField from "./FormField"; 

export{
  Card,
  FormField,
  Loader,
  
};