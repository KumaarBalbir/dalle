/* This code is importing the `Home` and `CreatePost` components from their respective files (`Home.js`
and `CreatePost.js`) and then exporting them as named exports. This allows other files to import and
use these components. */
import Home from "./Home";
import CreatePost from "./CreatePost";
export { Home, CreatePost };
